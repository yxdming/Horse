# =============================================================================
# RubikSQL GUI - Complete Python Requirements
# =============================================================================
# This file contains ALL dependencies needed for the RubikSQL GUI backend,
# including transitive dependencies from agent-heaven and rubiksql.
#
# For local development:
#   pip install -r requirements.txt
#   pip install -e /path/to/AgentHeaven-dev
#   pip install -e /path/to/RubikSQL-dev
#
# For packaging (PyInstaller):
#   pip install -r requirements.txt
#   pip install agent-heaven rubiksql  # from PyPI
# =============================================================================

# -----------------------------------------------------------------------------
# FastAPI Backend (RubikSQL-gui/backend)
# -----------------------------------------------------------------------------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
load_dotenv

# -----------------------------------------------------------------------------
# AgentHeaven Core Dependencies (from requirements.txt)
# -----------------------------------------------------------------------------
babel>=2.17.0
click>=8.0
cloudpickle>=2.0
dill>=0.3
diskcache>=5.0
docstring-parser>=0.15
isodate>=0.6
Jinja2>=3.0
litellm>=1.0
openai>=1.0
polib>=1.2.0
prettytable>=3.17.0
pyyaml>=6.0
requests>=2.28
tenacity>=8.0
termcolor>=2.0
prompt_toolkit>=3.0

# -----------------------------------------------------------------------------
# Database Drivers & SQL
# -----------------------------------------------------------------------------
asyncpg
duckdb-engine
duckdb>=1.0
psycopg2-binary  # Use binary for easier installation
pymysql
pymongo>=4.0
sqlalchemy>=2.0
sqlglot>=20.0

# -----------------------------------------------------------------------------
# LanceDB & LlamaIndex (for KB features)
# -----------------------------------------------------------------------------
fastmcp
lancedb
llama-index-core>=0.10
llama-index-vector-stores-lancedb
pyarrow

# -----------------------------------------------------------------------------
# NLP & Utilities
# -----------------------------------------------------------------------------
spacy
# pyahocorasick may require C++ build tools on Windows
# Install Visual Studio Build Tools if you get build errors
pyahocorasick

# -----------------------------------------------------------------------------
# LLM Providers
# -----------------------------------------------------------------------------
ollama

# -----------------------------------------------------------------------------
# PyInstaller (for packaging)
# -----------------------------------------------------------------------------
pyinstaller>=6.0
